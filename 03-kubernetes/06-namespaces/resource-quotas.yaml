apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-resources
  namespace: <namespace-name>
spec:
  hard:
    requests.cpu: "1"
    requests.memory: "1Gi"
    limits.cpu: "2"
    limits.memory: "2Gi"
    pods: "50"
    services: "10"
    persistentvolumeclaims: "10"